import { RudimentFilterPipe } from './rudiment-filter.pipe';

describe('RudimentFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RudimentFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
